# ES3C28P ESP-IDF skeleton

Target IDF 5.4.x, S3, 16 MB flash, octal PSRAM.

This tree compiles the link + HELLO loop. It does not vendor LVGL.
Use firmware/sidecar-arduino until you add LVGL and the ILI9341 driver.
